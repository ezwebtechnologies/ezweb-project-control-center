# Copy to `.env.local` and fill values. Never commit real secrets.

# Neon / PostgreSQL (sslmode=require recommended)
DATABASE_URL="postgresql://neondb_owner:npg_b6M7dfFrieLY@ep-purple-frost-aqvsp5b0.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Public site URL (SEO / metadata)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Gmail (quotation / transactional mail) — preferred when both are set
# Create an App Password: https://support.google.com/accounts/answer/185833
GMAIL_USER=
GMAIL_APP_PASSWORD=

# Optional display name + from address. For Gmail, use your address or a verified "Send mail as" alias.
# Example: "Quotations <you@gmail.com>"
QUOTATION_EMAIL_FROM=

# Resend (optional fallback if Gmail is not configured) — https://resend.com
RESEND_API_KEY=
