import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      variant="section"
      headline="Workspace"
      subline="Loading this section…"
    />
  );
}
