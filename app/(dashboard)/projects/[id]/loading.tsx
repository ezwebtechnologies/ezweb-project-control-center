import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      headline="Project"
      subline="Loading details, lifecycle, and playbooks…"
    />
  );
}
