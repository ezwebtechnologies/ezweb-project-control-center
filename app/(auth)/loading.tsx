import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      variant="fullscreen"
      headline="Sign in"
      subline="Preparing the sign-in experience…"
    />
  );
}
