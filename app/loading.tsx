import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading variant="fullscreen" headline="Loading" subline="Taking you to the control center…" />;
}
